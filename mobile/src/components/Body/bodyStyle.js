import { StyleSheet } from 'react-native'

export default StyleSheet.create({
    initialText:{
        flex: 0.8,
        height: '100%',
        justifyContent: 'center',
        alignItems: 'center',
        padding: 10
    },  
    text: {
        color: 'white',
        fontSize: 24,
        fontWeight: 'bold'
    }
})